/* Regenerated by the Dropbox sync GitHub Action (scripts/dropbox-sync.mjs).
   Until the first sync runs, this is an empty overlay so the seeded in-repo
   assets in assets/data/assets.js are used as-is. */
window.PORTAL_SYNCED = {};
